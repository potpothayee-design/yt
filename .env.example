# Everything works with NO keys. This file is 100% optional.
#
# Optional: a free Hugging Face read token (https://huggingface.co/settings/tokens).
# Only used to raise the quota of the optional AI-motion video engines.
# Without it the app still works — images via Pollinations (keyless) and the
# local cinematic-camera video renderer need nothing at all.
HF_TOKEN=
