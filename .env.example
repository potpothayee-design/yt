# ---------------------------------------------------------------
# EVERYTHING WORKS WITH NO KEYS AT ALL. This file is 100% optional.
# ---------------------------------------------------------------

# Optional: a free Hugging Face read token (https://huggingface.co/settings/tokens)
# Only used to unlock the Hugging Face video/image fallback engines.
# Without it the app still works using Pollinations (free, no key, unlimited).
HF_TOKEN=

# Optional: set to "/your-repo-name" when deploying to GitHub Pages
# NEXT_PUBLIC_BASE_PATH=/yt
