# ---- Web framework ----
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
python-multipart>=0.0.9
aiofiles>=23.2

# ---- Data ----
sqlalchemy>=2.0,<3.0
pydantic>=2.7
pydantic-settings>=2.3
email-validator>=2.1
psycopg2-binary>=2.9 ; platform_system != "Darwin" or platform_machine != "arm64"

# ---- Security ----
PyJWT>=2.8
cryptography>=42

# ---- Media pipeline ----
Pillow>=10.3
numpy>=1.26
scipy>=1.11             # formant filters for offline narration synthesis
imageio-ffmpeg>=0.5     # ships a self-contained ffmpeg binary (no system install needed)

# ---- HTTP / providers ----
httpx>=0.27

# ---- Storage (S3 / Cloudflare R2) ----
boto3>=1.34

# ---- YouTube Data API ----
google-api-python-client>=2.130
google-auth>=2.29
google-auth-oauthlib>=1.2
google-auth-httplib2>=0.2
